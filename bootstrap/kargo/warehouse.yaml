apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: nginx
  namespace: demo
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  shard: demo-agent
  subscriptions:
    - image:
        repoURL: nginx
        allowTagsRegexes:
          - '^1\.\d+\.\d+$'
